function [dist] = HSV_dist(X1, X2)



dh = min(abs(X1(:,1)-X2(:,1)), 1-abs(X1(:,1)-X2(:,1)));
ds = abs(X1(:,2) - X2(:,2));
dv = abs(X1(:,3) - X2(:,3));

dist = sqrt(dh.*dh + ds.*ds + dv.*dv);

   ( sin(h1)*s1*v1 - sin(h2)*s2*v2 )^2
 + ( cos(h1)*s1*v1 - cos(h2)*s2*v2 )^2
 + ( v1 - v2 )^2

end